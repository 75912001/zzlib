#pragma once

#include "lib_include.h"

/*
#include <lua.hpp>

typedef int  (*LUA_FN)(lua_State *L);

// Lua Engine
class lib_lua_t {
public:
	lib_lua_t();
	virtual ~lib_lua_t();
	int do_file(const char* file);
	int load_file(const char* file);
	int register_fn(const char* fn_name, LUA_FN lua_fn);

	lua_State* lua;
private:
	
};

extern lib_lua_t* g_lua;

*/